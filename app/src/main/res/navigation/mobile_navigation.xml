<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_recent">

    <fragment
        android:id="@+id/nav_recent"
        android:name="com.champignoom.paperant.ui.recent.RecentFragment"
        android:label="@string/menu_recent"
        tools:layout="@layout/fragment_recent" />

    <fragment
        android:id="@+id/nav_filepicker"
        android:name="com.champignoom.paperant.ui.filepicker.FilePickerFragment"
        android:label="@string/menu_filepicker"
        tools:layout="@layout/fragment_filepicker" />

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="com.champignoom.paperant.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_slideshow"
        tools:layout="@layout/fragment_slideshow" />
</navigation>